type Talk @model {
  id: ID!
  name: String!
  description: String
  date: String!
  speaker: Speaker! @connection(name: "SpeakerTalks")
}

type Speaker @model{
  id: ID!
  name: String!
  talks: [Talk] @connection(name: "SpeakerTalks")
}

